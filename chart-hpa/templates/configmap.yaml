apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hello-world-hpa.fullname" . }}-config
  labels:
    {{- include "hello-world-hpa.labels" . | nindent 4 }}
data:
  application.properties: |
{{- .Values.config | nindent 4 }}
