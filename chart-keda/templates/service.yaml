apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-world-keda.fullname" . }}
  labels:
    {{- include "hello-world-keda.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "hello-world-keda.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
